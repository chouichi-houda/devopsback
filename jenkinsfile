pipeline {
	    agent any
	    
	        environment {
        DOCKERHUB_CREDENTIALS = credentials('dockerHub')
        
        }
	    stages{
	        stage('Clone'){
	             steps{  
	                 git branch: 'rahma', url: 'https://github.com/chouichi-houda/devopsback.git'
	             }
	        }  
	       stage('MVN CLEAN') {
	            steps {
	                sh 'mvn clean'
	                 
	            }
	        }
	        stage('MVN COMPILE') {
	            steps {
	                   sh 'mvn compile -e'
	                 
	            }
	        }
		stage('Unit tests') {
                    steps {
                           sh 'mvn test'
                    }
		}
		stage('MVN SONARQUBE') {
            steps {
                sh 'mvn sonar:sonar -Dsonar.login=admin -Dsonar.password=sonar'
                 
            }
        }
        
        
	       stage('clean  package') {
            
            steps {
                sh 'mvn clean package'  
            }
     }
		
	       stage('Nexus'){
            steps{
                sh 'mvn deploy -DskipStaging=true'
            }
        }
        
          stage('Build image') {
           	steps {
       		 sh "docker build -t rahmachaabane/devopss-backend ."
       		}
       		}   
       		
       		
       	stage("login DockerHub") {
                steps{
                    sh 'echo adminnn123 | docker login -u rahmachaabane -p adminnn123'
                }
        }
        stage("Push to DockerHub") {
                steps{
                    sh 'docker push rahmachaabane/devopss-backend'
                }
        }	
       		   stage("Docker-compose") {
                steps{
                    sh 'docker-compose up -d'
                }
        }
       
		
	    }
	}

