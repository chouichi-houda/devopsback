pipeline {
    agent any
    stages {
        stage('clone') {
            steps {
                // Get some code from a GitHub repository
                git clone 'https://github.com/chouichi-houda/devopsback.git'}

            
        }
    }
}
