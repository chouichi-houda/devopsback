version: "3.3"
services:
      mysqldb:
       container_name: mysqldb
       image: mysql/mysql-server:8.0
       restart: unless-stopped
       environment:
          MYSQL_ROOT_PASSWORD: root
       ports:
        - "3307:3306"
      
      backend-container:
       image: chouichihouda/achatprojet
       container_name: tpachatback
       restart: on-failure
       ports:
         - "9090:8080"
       depends_on:
          - mysqldb